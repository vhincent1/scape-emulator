package net.scapeemulator.game.command

import net.scapeemulator.game.model.Player

fun defaultCommandHandler(name: String, player: Player, arguments: Array<String>) {
//    GameServer.INSTANCE.pluginManager.notify(CommandPluginEvent(name, player, arguments))
}